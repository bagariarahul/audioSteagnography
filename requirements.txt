pycryptodome
cryptography
2.  **Upload a Sample Audio**: The scripts reference a specific path (`C:\Users\Rahul\...`). You should change this line in your code to a relative path (e.g., `sample_audio.wav`) and upload a small `.wav` file to the GitHub repo so the code works out-of-the-box for anyone who clones it.
